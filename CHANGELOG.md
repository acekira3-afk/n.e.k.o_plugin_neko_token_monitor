# 更新日志

## 0.8.0 · 2026-09-23

### 多模型猫粮总览

- 新增 16 个来源入口，各自保存配置、密钥、查询结果和更新时间；已配置自动来源持续刷新。
- 在挂件菜单打开「模型额度总览」，查看卡片并一键切换「气泡展示」，气泡标题仍叫「猫粮余额」。
- Codex 显示各额度窗口的剩余百分比、进度条和重置时间。
- 新增 OpenRouter 共享账户余额（需 Management Key），以总购入额度减总用量；新增硅基流动账户总余额（含充值余额）。
- 保留 DeepSeek 官方余额、GPT / OpenAI API 和 Claude API 的自定预算减组织费用、JEV 本机用量、自定义接口及手动金额。
- ChatGPT、Claude / Claude Code、Gemini、Kimi、通义千问、豆包、智谱 GLM 增加独立手动额度记录：剩余、总量、次/token/百分比及重置时间备注。**这些入口尚未接入自动查询**，不会自动扣减或重置，也不表示平台没有提供其他接入方式。
- 不同平台额度不累加；共享账户余额不冒充单模型独立额度；预算不冒充官方余额。查询失败保留旧值，未接入不显示为零。
- 首次升级自动迁移旧版当前来源，原 JEV 统计继续保留。切换平台不会把密钥发给另一个平台。

### 验证

- 12 项测试通过，覆盖新接口格式、余额计算、旧数据迁移、密钥隔离、独立额度和 JEV 统计。
- 本机浏览器验证 16 张卡片、保存、切换、窄窗口布局与脚本错误检查。
- OpenRouter / 硅基流动通过官方接口结构模拟验证；未使用真实账户密钥做联网验收。Windows 新面板尚未完整实机验收。

## 0.7.2 · 2026-09-23

本次公开更新包含此前仅在本机测试的 0.7.1 功能。

### 新增：Codex 当前账号额度监控

- 在「账户设置 → 查询方式」中选择「Codex · 当前登录账号套餐额度」。
- 通过本机 Codex 的官方只读接口读取套餐剩余百分比和重置时间；默认每 5 分钟查询，可在设置中调整。
- 同时返回多个额度窗口时，主气泡显示剩余百分比最低的一项，旁边显示其他窗口。
- 无需向猫粮填写或复制登录密钥。需要本机安装可启动的 Codex CLI / app-server，并已登录需要监控的账号。
- 查询失败保留上次结果并标注；未返回的额度不会被当作零。

这项数据是 **Codex 套餐额度**，不等于现金余额、ChatGPT 聊天剩余次数或精确的剩余 token 数。界面标题仍为「猫粮余额」。

### 新增：JEV 本机用量记录

- 接收本机 JEV 桥接上报的真实输入、输出 token 数，并显示累计用量。
- 同一请求标识重复上报会去重；最近 2,000 个请求标识用于去重，累计计数保存在本机。
- 只接收服务商、模型、请求标识和用量，不需要上报提示词、回答或 API Key。
- 需要配套桥接上报；不会自动统计所有 JEV 客户端、其他电脑或接入前的历史。已运行的旧桥接需重启后才加载上报逻辑。
- 未找到 JEV 的公开余额查询接口，因此显示「未提供」，不会把已用 token 伪装成剩余余额。

### 保留的功能

DeepSeek 官方余额、OpenAI/GPT 和 Claude 组织费用预算、自定义 HTTPS 余额接口、手动预算、窗口内设置、YUI 点击弹跳与台词、空闲状态切换、互动计数及 YUI 记忆导入。

### 验证与范围

- 本地 8 项插件测试通过，包括额度换算、缺失数据处理、用量去重和持久化。
- Codex 额度已在 macOS 本机实测，并与 Codex 应用查询结果核对一致。
- JEV 已完成真实请求 → 本机桥接 → 猫粮显示的端到端验证，重复缓存调用未重复计数。
- Windows 窗口沿用 0.7.0 的构建；新增 Codex 额度功能尚未在 Windows 完整 NEKO 环境实测。

## 0.7.0 · 2026-09-23

首次公开版本：多服务商余额/预算设置、YUI 桌面挂件、窗口内账户设置、点击弹跳和带喵台词、Agent 空闲状态、记录模式及 YUI 记忆保存。安装包包含 macOS 通用窗口程序和 Windows x64 窗口程序。
